import 'package:flutter/material.dart';

class AppShadowPalette {
  static List<BoxShadow> get standartCardShadow => [
        const BoxShadow(
          color: Color(0x24455B63),
          blurRadius: 14,
          offset: Offset(0, 5),
          spreadRadius: 0,
        ),
      ];
}
